import styled from 'styled-components';

export const div = styled.div`
   width: 100%;
   height: 100vh;
   display: flex;
   align-items: center;
   justify-content: center;
   /* background-color: var(--smooth-sky-cl); */
`;

export const centerDiv = styled.div`
   width: 900px;
   background-color: var(--spec-suggested-action);
   border-radius: 8px;
`;
