import styled from "styled-components";

export const div = styled.div`
   padding: 1rem;
   /* border: 1px dashed var(--spec-text-secondary); */
   height: 400px;
   overflow-x: hidden;
   border: 1px solid var(--spec-text-disabled);
`;
