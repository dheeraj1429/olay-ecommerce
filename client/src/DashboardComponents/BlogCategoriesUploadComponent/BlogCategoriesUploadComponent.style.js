import styled from 'styled-components';

export const div = styled.div`
   .flex {
      display: flex;
      align-items: center;
      p {
         margin: 0;
         color: var(--main-cl);
      }
   }
`;
