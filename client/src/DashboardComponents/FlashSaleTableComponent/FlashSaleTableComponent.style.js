import styled from 'styled-components';

export const div = styled.div`
   .center_heading {
      display: flex;
      width: 100%;
      align-items: center;
      justify-content: center;
      padding: 1rem;

      p {
         font-size: 20px;
         color: var(--main-cl);
      }
   }
`;
