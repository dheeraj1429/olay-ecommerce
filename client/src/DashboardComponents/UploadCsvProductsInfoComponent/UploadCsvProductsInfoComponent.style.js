import styled from "styled-components";

export const div = styled.div`
   border-bottom: 1px solid var(--spec-icon-inactive);
   margin-top: 2rem;
   h5 {
      font-size: 18px;
      font-weight: 400;
      margin-bottom: 1rem;
   }
   .heading_div {
      display: flex;

      p {
         margin: 0;
         margin-bottom: 0.5rem;
         font-size: 14px;
      }
   }
   .space {
      width: 80px;
   }
`;

export const infoDiv = styled.div`
   width: 100%;
   padding: 0.5rem 1rem;
   border: transparent;
`;
